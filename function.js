function add_two(num1,num2){
    return num1+num2;
}

const number1=5;
const number2=25;
console.log(add_two(number1,number2));

function greet(f_name,l_name,age){
    return `${f_name} ${l_name} is ${age} years old`;
}

const first_name=`Joswin`;
const last_name=`Emmanuel`;
const age=19;
console.log(greet(first_name,last_name,age));